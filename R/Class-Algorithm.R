setClass(
  
  # Set the name for the class
  Class = "Algorithm",
  
  # Define the slots
  slots = c(
    name = "character",
    parameters = "character",
    output = "character",
    description = "character"
  )
  
)